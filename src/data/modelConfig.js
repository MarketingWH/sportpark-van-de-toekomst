export const modelConfig = {
  // Zet deze op true zodra je eigen GLB-bestand aanwezig is.
  useGlb: false,

  // Plaats je model in /public/models/ en houd deze bestandsnaam aan,
  // of pas dit pad aan naar je eigen GLB-locatie.
  src: '/models/sportpark-van-de-toekomst.glb',

  // Gebruik deze transform om je model netjes in de viewer te leggen.
  position: [0, 0, 0],
  rotation: [0, 0, 0],
  scale: 1,
};
